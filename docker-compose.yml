version: '3.8'

services:
  smartscore-mspi:
    build: .
    ports:
      - "4000:3001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: always
